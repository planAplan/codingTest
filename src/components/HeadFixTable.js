import React, {Component} from 'react';

export default class HeadFixTable extends Component {
    render () {
        return (
            <div>
                HeadFixTable
            </div>
        );
    }
}